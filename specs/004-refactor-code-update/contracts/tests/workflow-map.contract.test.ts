import { fail } from 'assert';

describe('Contract: ipc.workflow-map.sync', () => {
  it('returns unchanged workflow when versions match', () => {
    fail('not implemented');
  });

  it('returns updated workflow when renderer version is stale', () => {
    fail('not implemented');
  });

  it('throws workflow/not-found when flowId is invalid', () => {
    fail('not implemented');
  });
});
